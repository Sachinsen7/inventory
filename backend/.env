############################################
# DO NOT COMMIT SECRETS TO SOURCE CONTROL  #
############################################
# This file previously contained secrets that were committed to the repository.
# Those values have been redacted here. Replace these values locally and DO NOT
# commit this file. Ensure `backend/.env` is included in .gitignore.

PORT=5000
BACKEND_URL=http://localhost:5000
# Provide your MongoDB connection string here (do NOT include username/password in repo)
MONGODB_URI=
# Use a strong random secret (e.g. generated by `openssl rand -base64 32`)
JWT_SECRET=
ADMIN_USERNAME=admin
ADMIN_PASSWORD=
