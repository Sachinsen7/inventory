# Example environment variables for the backend
# Copy this file to `.env` and fill the values locally (do NOT commit `.env`).

PORT=5000
BACKEND_URL=http://localhost:5000

# Production HTTPS Configuration:
# - SERVER_HOST: domain name for HTTPS (used in startup logging)
# - NODE_ENV: set to 'production' to enable HTTP->HTTPS redirect, HSTS headers, secure cookies
# Example: SERVER_HOST=api.yourdomain.com
SERVER_HOST=

# MONGODB_URI example (do not commit credentials):
# mongodb+srv://<username>:<password>@cluster0.xxxx.mongodb.net/<dbname>?retryWrites=true&w=majority
MONGODB_URI=

# Strong JWT secret (generate with a secure RNG, keep secret):
JWT_SECRET=

# Admin credentials (use secure password; do not keep default values in production)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=

# CORS: Comma-separated list of allowed origins (use exact URLs, no wildcards)
# Local dev example: http://localhost:3000,http://localhost:5000
# Production: https://yourdomain.com,https://www.yourdomain.com
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5000

# NODE_ENV: development or production
# - development: debug logs enabled, HTTP allowed, cookies not secured
# - production: debug logs disabled, HTTP->HTTPS redirect, HSTS headers, secure cookies (httpOnly, Secure, SameSite=Strict)
NODE_ENV=development

# Other env vars (add as needed)
